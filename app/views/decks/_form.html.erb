<%= form_for(deck) do |f| %>
  <div class="row">
    <div class="col-md-9">
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, placeholder: "Deck name" %>
      </div>
    </div>
  </div>
  <div class="submit">
    <%= f.submit %>
  </div>
<% end %>